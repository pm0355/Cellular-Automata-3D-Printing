
function observe= observe(c,d,e)
     global x;
      global y;
       global z;
 mesh(c,d,e);
  %contourf(c,1);
  %subplot(2,1,1);
  %colormap(gray);
  %subplot(2,1,2);
  %x= [c(:);c(:);c(:)];
  %y=[d(:);d(:);d(:)];
  %z=[e(:);e(:);e(:)];
  %scatter(x,y,z);
  %scatter(x,z);
%contourf(c,400)
%imagesc(e/sqrt(c^2-d^2))
end